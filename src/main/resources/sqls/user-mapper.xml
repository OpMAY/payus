<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.demo.mapper.UserMapper">
    <select id="getUserInfoByUserNoForHome" resultType="HomeUser">
        select user_no, name, point, profile_img
        from user
        WHERE user_no = #{user_no}
    </select>
    <select id="getUserLogin" resultType="LoginCheck">
        select user_no, bank_name, bank_account, bank_owner
        from user
        WHERE access_token = #{access_token}
          AND sns = #{sns}
    </select>
    <insert id="registerUser" useGeneratedKeys="true" keyProperty="user_no">
        insert into user(name, access_token, sns, reg_date)
        VALUES (#{name}, #{access_token}, #{sns}, #{reg_date})
    </insert>
</mapper>